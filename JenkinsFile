pipeline {
  agent { docker { image 'python:3.7.3' } }
  stages {
    stage('myStage'){
      steps {
        sh 'ls -la' 
      }
    }
    stage('Build') {
      steps { 
        sh 'ls' 
      }
    }
    stage('nextStage') {
        steps {
            sh 'python3 ./streamset_pipelines.py'
        }
    }
  }
}